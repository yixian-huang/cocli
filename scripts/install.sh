#!/usr/bin/env sh
# cocli install.sh — placeholder.
#
# This will become a curl-able install script in M0.0.4 (soft launch).
# Until then there are no pre-built binaries to fetch. Build from source:
#
#   git clone https://github.com/yixian-huang/cocli.git
#   cd cocli
#   cargo build --release --bin cocli
#   ./target/release/cocli --version

echo "cocli pre-built binaries not yet available."
echo "See https://github.com/yixian-huang/cocli for build-from-source instructions."
echo "Support matrix: docs/SUPPORT_MATRIX.md"
exit 1
