# cross-rs config for CI aarch64-linux builds.
# openssl-sys needs the arm64 OpenSSL development package inside the cross image.

[target.aarch64-unknown-linux-gnu]
pre-build = [
  "dpkg --add-architecture arm64",
  "apt-get update && apt-get install --assume-yes libssl-dev:arm64 pkg-config",
]
